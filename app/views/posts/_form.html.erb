  <%= link_to 'Back', posts_path, class: "btn btn-outline-success" %>

<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group row">
    <label for="staticTitle" class="col-sm-2 col-form-label">Title</label>
    <div class="col-sm-10">
      <%= form.text_field :title ,class: "form-control",id: "inputPassword", placeholder: "Title",:required => true%>
    </div>
  </div>
  <div class="form-group row">
    <label for="inputTitle" class="col-sm-2 col-form-label">Description</label>
    <div class="col-sm-10">
      <%= form.text_area :description, class: "form-control",placeholder: "description", :required => true %>
    </div>
  </div>
  <div class="form-group row">
    <label for="inputPassword" class="col-sm-2 col-form-label">Images</label>
    <div class="col-sm-10">
      <%= form.file_field :image, multiple: true %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-secondary"%>
  </div>
<% end %>
<style type="text/css">
  .actions{
    position: absolute;
    margin-left: 190px;
  }
</style>